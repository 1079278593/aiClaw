# hyxClaw 环境变量配置

# 用户数据目录（必需）
# 所有数据（配置、会话、记忆、知识库等）都存储在此目录
# Windows 示例: HYXCLAW_DATA_DIR=D:\myData\myKB
# Linux/macOS 示例: HYXCLAW_DATA_DIR=/home/user/.myKB
#
# 数据目录下三个常用子目录：
#   inputs/          收件箱，放入待处理的原文、图片、PDF
#   knowledge_base/  知识库，整理后的 Markdown 笔记
#   files/           程序工作台（提示词、命令、统计），不要堆文档
# 流转：资料放入 inputs/ → 让 AI 阅读总结 → 写入 knowledge_base/
# Trilium 在 config.json 的 tools.trilium 中配置（只读），不要把数据目录指到 Trilium 安装路径
HYXCLAW_DATA_DIR=D:\myData\myKB
